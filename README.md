Data Scientist - Getting and Cleaning Data

After collecting the data from the presented URL, the training and test sets are merged in one data set.

To further use only the mean and standard diviation are kept.

The ids of the activity are converted to the human readable activities.

The labels are made more descriptive.

The final data set includes the mean per subject and activity in each variable.
